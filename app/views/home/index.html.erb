<div class="max-w-md mx-auto mt-10 px-4">
  <div class="bg-white shadow-md rounded-lg p-6 text-center">

    <h2 class="text-lg font-bold mb-4">
      サービスの説明
    </h2>

    <p class="text-sm text-gray-600 mb-6">
      予定を「まあいいか」で終わらせないための自己管理サポートアプリです。
    </p>

    <% if user_signed_in? %>
      <!-- ログイン後に表示 -->
      <p class="text-sm text-gray-700 mb-4">
        ログイン中です
      </p>

      <%= button_to destroy_user_session_path,
                    method: :delete,
                    data: { turbo: false },
                    class: "inline-block bg-gray-500 text-white font-semibold px-6 py-3 rounded-md hover:bg-gray-600" do %>
        ログアウト
      <% end %>

    <% else %>
      <!-- 未ログイン時に表示 -->
      <%= button_to user_line_omniauth_authorize_path,
                    method: :post,
                    data: { turbo: false },
                    class: "inline-block bg-green-500 text-white font-semibold px-6 py-3 rounded-md hover:bg-green-600" do %>
        LINEログイン
      <% end %>
    <% end %>

  </div>
</div>

